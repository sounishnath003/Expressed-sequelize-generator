/** Will configure the association Mappings (1:N) / (M:N) / (N:1)  */
function buildAssociationsBetweenSchemas() {

}

/** Add all schema you want to export!! */
const Schema = {};

/** Export Schema from index.ts file */
export {
    Schema ,
    buildAssociationsBetweenSchemas
};
